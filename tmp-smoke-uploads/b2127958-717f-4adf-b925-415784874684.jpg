:root {
  --bg: #f4f7fb;
  --panel: #ffffff;
  --ink: #162033;
  --muted: #667085;
  --line: #d9e2ec;
  --green: #2f6b45;
  --teal: #169b8f;
  --blue: #13294b;
  --orange: #ff7a1a;
  --rose: #b44762;
  --shadow: 0 18px 48px rgba(19, 41, 75, .09);
  --soft-shadow: 0 10px 28px rgba(19, 41, 75, .07);
}

* { box-sizing: border-box; }

body {
  margin: 0;
  min-height: 100vh;
  background: linear-gradient(180deg, #eef5f8 0%, var(--bg) 34%, #f8faf7 100%);
  color: var(--ink);
  font-family: Inter, ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, Arial, sans-serif;
}

button, input, select, textarea { font: inherit; }

.app-shell {
  display: grid;
  grid-template-columns: 292px minmax(0, 1fr);
  min-height: 100vh;
}

.sidebar {
  display: flex;
  flex-direction: column;
  gap: 20px;
  padding: 24px;
  background: #13294b;
  color: #fff;
  border-right: 1px solid rgba(255,255,255,.1);
}

.brand {
  display: flex;
  align-items: center;
  gap: 14px;
  padding: 14px;
  border-radius: 8px;
  background: rgba(255,255,255,.08);
  border: 1px solid rgba(255,255,255,.12);
}

.brand-mark {
  display: grid;
  place-items: center;
  width: 42px;
  height: 42px;
  border-radius: 8px;
  background: #f0c35a;
  color: #24362b;
  font-weight: 900;
}

.brand-logo {
  display: block;
  width: 74px;
  height: 56px;
  object-fit: contain;
  border-radius: 8px;
  background: #fff;
  padding: 7px;
  box-shadow: 0 8px 18px rgba(0,0,0,.14);
}

.brand strong, .brand small { display: block; }
.brand strong { font-size: 16px; letter-spacing: .01em; }
.brand small { color: rgba(255,255,255,.7); margin-top: 2px; }

.tabs {
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.tab {
  width: 100%;
  border: 1px solid rgba(255,255,255,.12);
  background: rgba(255,255,255,.07);
  color: #fff;
  border-radius: 8px;
  padding: 13px 14px;
  text-align: left;
  cursor: pointer;
  font-weight: 800;
  transition: background .16s ease, color .16s ease, transform .16s ease;
}

.tab:hover, .tab.active {
  background: #fff;
  color: var(--blue);
  transform: translateY(-1px);
}

.sidebar-note {
  margin-top: auto;
  color: rgba(255,255,255,.76);
  line-height: 1.5;
  font-size: 14px;
  padding: 14px;
  border-radius: 8px;
  background: rgba(255,255,255,.07);
}

.workspace {
  min-width: 0;
  padding: 34px;
  max-width: 1180px;
}

.page-head {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 16px;
  margin-bottom: 22px;
}

.eyebrow {
  margin: 0 0 6px;
  color: var(--orange);
  font-size: 13px;
  font-weight: 800;
  text-transform: uppercase;
}

h1 {
  margin: 0;
  font-size: 32px;
  line-height: 1.15;
  color: var(--blue);
}

h3 { margin: 0 0 8px; }
p { line-height: 1.55; }

.form-grid, .editor-panel, .panel {
  background: var(--panel);
  border: 1px solid var(--line);
  border-radius: 8px;
  box-shadow: var(--shadow);
}

.form-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 18px;
  padding: 24px;
}

label {
  display: flex;
  flex-direction: column;
  gap: 7px;
  color: #26364d;
  font-size: 14px;
  font-weight: 750;
}

.wide { grid-column: 1 / -1; }

input, select, textarea {
  width: 100%;
  border: 1px solid #cdd8e4;
  border-radius: 8px;
  background: #fbfdff;
  color: var(--ink);
  padding: 13px 14px;
  box-shadow: inset 0 1px 0 rgba(255,255,255,.75);
}

textarea { resize: vertical; }

input:focus, select:focus, textarea:focus {
  outline: 3px solid rgba(22, 155, 143, .18);
  border-color: var(--teal);
}

.primary-action, .secondary-action, .btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-height: 42px;
  border: 0;
  border-radius: 8px;
  padding: 0 16px;
  text-decoration: none;
  font-weight: 800;
  cursor: pointer;
}

.primary-action, .btn {
  background: var(--orange);
  color: #fff;
  box-shadow: 0 10px 20px rgba(255, 122, 26, .22);
}

.secondary-action {
  background: #fff;
  color: var(--blue);
  border: 1px solid var(--line);
  box-shadow: var(--soft-shadow);
}

button:disabled {
  opacity: .72;
  cursor: wait;
}

.error {
  border: 1px solid #f0b5b5;
  background: #fff1f1;
  color: #982b2b;
  padding: 13px 14px;
  border-radius: 8px;
  margin-bottom: 16px;
}

.status-pill, .type-chip {
  display: inline-flex;
  align-items: center;
  min-height: 28px;
  border-radius: 999px;
  padding: 0 10px;
  background: #fff2e8;
  color: #ad4a08;
  font-size: 13px;
  font-weight: 800;
}

.muted-chip {
  margin-left: 6px;
  background: #eef1f6;
  color: var(--blue);
}

.upload-box {
  border: 1px dashed #9fb3c8;
  border-radius: 8px;
  padding: 16px;
  background: #f8fbff;
}

.photo-preview {
  width: 160px;
  height: 120px;
  object-fit: cover;
  border-radius: 8px;
  border: 1px solid var(--line);
}

.toolbar {
  display: grid;
  grid-template-columns: minmax(0, 1fr) auto;
  gap: 12px;
  align-items: center;
  margin-bottom: 14px;
}

.toolbar span {
  color: var(--muted);
  font-weight: 700;
}

.table-wrap {
  overflow-x: auto;
  background: var(--panel);
  border: 1px solid var(--line);
  border-radius: 8px;
  box-shadow: var(--shadow);
}

table {
  width: 100%;
  border-collapse: collapse;
}

th, td {
  text-align: left;
  padding: 14px;
  border-bottom: 1px solid #edf1ed;
  vertical-align: middle;
  font-size: 14px;
}

th {
  color: #52637a;
  font-size: 12px;
  text-transform: uppercase;
}

.content-layout {
  display: grid;
  grid-template-columns: 260px minmax(0, 1fr);
  gap: 18px;
}

.type-list {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.type-button {
  border: 1px solid var(--line);
  border-radius: 8px;
  background: #fff;
  color: var(--ink);
  padding: 12px;
  text-align: left;
  cursor: pointer;
}

.type-button.active {
  border-color: rgba(255,122,26,.45);
  background: #fff3e8;
  color: #a84608;
  font-weight: 800;
}

.editor-panel {
  display: grid;
  gap: 14px;
  padding: 20px;
}

.info-grid {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 16px;
}

.method-strip {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  align-items: center;
  margin-bottom: 16px;
}

.method-strip strong,
.method-strip span {
  display: inline-flex;
  align-items: center;
  min-height: 32px;
  border-radius: 999px;
  padding: 0 12px;
  font-size: 13px;
  line-height: 1;
}

.method-strip strong {
  background: var(--green);
  color: #fff;
}

.method-strip span {
  background: #fff;
  border: 1px solid var(--line);
  color: var(--muted);
  font-weight: 700;
}

.info-grid article {
  min-height: 150px;
  background: #fff;
  border: 1px solid var(--line);
  border-radius: 8px;
  padding: 18px;
  box-shadow: var(--soft-shadow);
}

.info-grid p {
  margin: 0;
  color: var(--muted);
}

@media (max-width: 860px) {
  .app-shell {
    grid-template-columns: 1fr;
  }

  .sidebar {
    min-height: auto;
    padding: 16px;
    gap: 14px;
  }

  .brand {
    padding: 10px;
  }

  .brand-logo {
    width: 58px;
    height: 44px;
  }

  .tabs {
    flex-direction: row;
    overflow-x: auto;
  }

  .tab {
    white-space: nowrap;
  }

  .sidebar-note {
    display: none;
  }

  .workspace {
    padding: 18px;
  }

  .form-grid, .content-layout, .info-grid {
    grid-template-columns: 1fr;
  }

  .page-head, .toolbar {
    grid-template-columns: 1fr;
    display: grid;
  }

  h1 {
    font-size: 24px;
  }
}
